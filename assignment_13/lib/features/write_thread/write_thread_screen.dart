import 'package:flutter/material.dart';

class WriteThreadScreen extends StatelessWidget {
  const WriteThreadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("WRTIE THREAD"),
        centerTitle: true,
      ),
    );
  }
}
